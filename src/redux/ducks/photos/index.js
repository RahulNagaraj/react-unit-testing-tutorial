import reducer from './reducers';

export { default as photoSelectors } from './selectors';
export { default as photoActions } from './actions';
export { default as photoTypes } from './types';

export default reducer;
